{
  "cli": "ng",
  "id": "prepare-config",
  "type": "object",
  "properties": {
    "configName": {
      "type": "string",
      "description": "請選擇目標模組(初始化使用Base)",
      "enum": ["base"]
    }
  },
  "required": ["configName"]
}
