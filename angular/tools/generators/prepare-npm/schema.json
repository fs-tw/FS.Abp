{
  "cli": "ng",
  "id": "prepare-npm",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Add package to npm folder(Dev for public,Volo for commercial)",
      "enum": ["Volo", "Dev"],
      "default":"Volo"
    }
  },
  "required": ["name"]
}
