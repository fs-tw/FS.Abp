{
  "cli": "ng",
  "id": "init-flow",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Add package to npm folder(Dev for public,Volo for commercial),*warning delete and remove",
      "enum": ["volo", "dev"]
    },
    "configName": {
      "type": "string",
      "description": "請選擇目標模組(初始化使用Base)",
      "enum": ["base"]
    }
  },
  "required": ["name", "configName"]
}
