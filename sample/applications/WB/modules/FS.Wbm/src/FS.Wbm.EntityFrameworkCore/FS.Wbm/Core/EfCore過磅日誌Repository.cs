//------------------------------------------------------------------------------
// This code was generated by Entity Developer tool using the template for generating Repositories and a Unit of Work for EF Core model.
// 4.0.0
//
//------------------------------------------------------------------------------
using System;
using System.Linq;
using System.Collections.Generic;
using Microsoft.EntityFrameworkCore;
using System.Threading.Tasks;

namespace FS.Wbm.Core
{
    public partial class EfCore過磅日誌Repository : I過磅日誌Repository
    {

        public async Task<List<過磅日誌>> GetByAsync(DateTime startDate, DateTime endDate)
        {
            var q = await this.GetQueryableAsync();

            return q
                //.Where(x => x.日期 == startDate.Date )
                .Where(x => x.日期 >= startDate.Date && x.日期 <= endDate.Date)
                .Where(x => x.內容.Contains("過磅完成"))
                .OrderBy(x=>x.日期).ThenBy(x=>x.時間)
                .ToList();

        }
        public async Task<List<過磅日誌>> GetFirstAsync(DateTime startDate, DateTime endDate)
        {
            var q = await this.GetQueryableAsync();

            return q
                .Where(x => x.日期 >= startDate.Date && x.日期 <= endDate.Date)
                .Where(x => x.內容.Contains("過磅完成") || x.內容.Contains("過磅完成"))
                .Where(x => !x.內容.Contains("被退運"))
                .OrderBy(x => x.日期).ThenBy(x => x.時間)
                .ToList();
        }
    }
}
