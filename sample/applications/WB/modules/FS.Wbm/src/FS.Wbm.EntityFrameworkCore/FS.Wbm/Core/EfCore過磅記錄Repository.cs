//------------------------------------------------------------------------------
// This code was generated by Entity Developer tool using the template for generating Repositories and a Unit of Work for EF Core model.
// 4.0.0
//
//------------------------------------------------------------------------------
using System;
using System.Linq;
using System.Collections.Generic;
using Microsoft.EntityFrameworkCore;
using System.Threading.Tasks;

namespace FS.Wbm.Core
{
    public partial class EfCore過磅記錄Repository : I過磅記錄Repository
    {
        public async Task<List<過磅記錄>> GetByAsync(DateTime startDate, DateTime endDate)
        {
            var q = await this.GetQueryableAsync();

            return q
                .Where(x => x.日期 >= startDate.Date && x.日期 <= endDate.Date)
                .OrderBy(x => x.日期).ThenBy(x => x.時間)
                .ToList();

        }
    }
}
