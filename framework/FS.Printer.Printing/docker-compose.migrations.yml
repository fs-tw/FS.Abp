version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=Printing_Identity
      - Printing_DB=Printing_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
